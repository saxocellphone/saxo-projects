# Static web app: I Am Working.
# Org-specific language rules layer on top via macros / WORKSPACE config.
filegroup(
    name = "i-am-working",
    srcs = glob(
        [
            "**/*",
        ],
        exclude = [
            "BUILD.bazel",
            "PROJECT.yaml",
        ],
    ),
    visibility = ["//visibility:public"],
)
