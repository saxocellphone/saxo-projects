# Generated by "runko project create" (build capability, docs/design.md
# §14.5.4). Target pattern for this project: //hello/...
#
# Org-specific language rules (go_library, py_library, ...) layer on top via
# org templates - this file is deliberately minimal so create_project never
# requires hand-authored BUILD content (principle 8, anti-Boq, §2.3).

filegroup(
    name = "srcs",
    srcs = glob(["**/*"], exclude = ["BUILD.bazel"]),
    visibility = ["//visibility:public"],
)
